#include <stdio.h>
int main()
{
    int a = 7, b = 9, c = 3, result;

    printf("(a == b) && (c > b) is %d \n", result);

    printf("(a == b) || (c < b) is %d \n", result);

    printf("!(a != b) is %d \n", result);

    return 0;
}
